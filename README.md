# kryoss-assignment
# Contact Management API

A simple **Flask + SQLAlchemy** CRUD application to manage contacts.  
Supports creating, reading, updating, and deleting contacts with basic validation.

---

## 🚀 Features
- **Create**: Add a new contact
- **Read**: Display all contacts or a single contact
- **Update**: Edit an existing contact
- **Delete**: Remove a contact
- **Validation**: Email format, phone length, required fields
- **Error Handling**: Clear messages (e.g., "Email already exists")
- **Persistence**: Data stored in SQLite (can be swapped for PostgreSQL/MySQL)
- **API Design**: RESTful endpoints
- **UI/UX (optional)**: Can be extended with HTML templates + Bootstrap


